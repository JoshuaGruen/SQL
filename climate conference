this code that will display various climate conference.

SELECT events.event_id,hotels.hotel_name
FROM events
JOIN hotels
ON events.event_id=hotels.hotel_id
;

SELECT member_id
FROM reservations
LIMIT 5
;

SELECT vips.member_id,hotels.hotel_id
FROM vips
JOIN hotels
ON vips.member_since
LIMIT 5
;

SELECT reservations.hotel
FROM reservations
;

SELECT *
FROM hotels
;
